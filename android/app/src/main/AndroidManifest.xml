<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:installLocation="auto">

    <uses-feature android:glEsVersion="0x00020000" />

    <!-- Input comes from SDL gamepads only. There is no on-screen overlay and
         no touch handling compiled in at all (PORT_NO_TOUCH_CONTROLS), so the
         touchscreen is declared not-required alongside the transports a
         controller arrives over. The gamepad feature stays non-required too,
         since controllers are normally paired after install. -->
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <!-- Gamepad rumble, through SDL's haptics path. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:label="@string/app_name"
        android:allowBackup="false"
        android:theme="@style/TmcTheme"
        android:enableOnBackInvokedCallback="false"
        android:hardwareAccelerated="true">

        <activity
            android:name=".TmcActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="sensorLandscape"
            android:launchMode="singleInstance"
            android:alwaysRetainTaskState="true"
            android:preferMinimalPostProcessing="true"
            android:configChanges="layoutDirection|locale|grammaticalGender|fontScale|fontWeightAdjustment|orientation|uiMode|screenLayout|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- So a USB controller plugged in while the app is closed still
                 reaches SDL's joystick layer. -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
        </activity>
    </application>
</manifest>
