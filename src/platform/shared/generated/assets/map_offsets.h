/* Host-port stub for the auto-generated offset table normally produced
 * by the asset_processor during the GBA ROM build. The SDL port must
 * not touch `assets/` (see docs/sdl_port.md, "Coexistence"), so the
 * stub lives under `src/platform/shared/generated/` and is placed onto
 * the include search path for the SDL build only.
 */
#include "_port_offset_stubs.h"
